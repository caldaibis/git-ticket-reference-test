# Configuratie voor de Git ticket hooks.
# Vul de relevante velden in voor het platform dat je gebruikt.

# (Optioneel) Comma-gescheiden lijst van reguliere expressies om ticket-ID's te herkennen.
# Voorbeeld: TICKET_REGEX=[A-Z]+-[0-9]+,#\d+
TICKET_REGEX=

# Het te gebruiken platform voor ticket-validatie: 'gitlab', 'github', of 'azure'.
# Laat leeg om de online validatie over te slaan.
TICKET_PLATFORM=

# --- GitLab ---
# Persoonlijke access token met 'read_api' scope.
# Te genereren via: https://gitlab.com/-/profile/personal_access_tokens
GITLAB_TOKEN=
# Het numerieke Project ID, te vinden in de projectinstellingen op GitLab.
GITLAB_PROJECT_ID=

# --- GitHub ---
# Persoonlijke access token met 'repo' scope.
# Te genereren via: https://github.com/settings/tokens
GITHUB_TOKEN=
# De repository in het formaat 'organisatie/repository'.
GITHUB_REPO=

# --- Azure DevOps ---
# Persoonlijke access token met 'Work Items (Read)' scope.
# Genereer een PAT en encodeer deze in Base64 met het commando:
# echo -n ":<PAT>" | base64
AZURE_TOKEN=
# Naam van de Azure DevOps-organisatie.
AZURE_ORG=
# Naam van het project binnen de organisatie.
AZURE_PROJECT=